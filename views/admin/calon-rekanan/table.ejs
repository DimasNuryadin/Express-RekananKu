<table id="example2" class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>No</th>
      <th>Nama Perusahaan</th>
      <th>Bidang Usaha</th>
      <th>Status</th>
      <th>Aksi</th>
    </tr>
  </thead>
  <tbody>
    <% for(let i=0; i < statusRekanan.length; i++) { %>
      <tr>
        <td>
          <%= i + 1 %>
        </td>
        <td>
          Bidang Usaha
        </td>
        <td>
          Nama Perusahaan
        </td>
        <td>
          <%= statusRekanan[i].status %>
        </td>
        <td class="d-flex gap-5">
          <a class="btn btn-light mr-2" href="/calon-rekanan/view-user/<%= statusRekanan[i].userId %>">Lihat</a>
          <form action="/calon-rekanan/accept/<%= statusRekanan[i]._id %>?_method=PUT" method="POST" class="mr-2">
            <button class="btn btn-success">Terima</button>
          </form>
          <form action="/calon-rekanan/reject/<%= statusRekanan[i]._id %>?_method=PUT" method="POST">
            <button class="btn btn-danger">Tolak</button>
          </form>
        </td>
      </tr>
      <% } %>
  </tbody>
</table>